# Canonical production origin, used for metadata (canonical URLs, Open Graph,
# sitemap, robots). Set this in the Vercel project so shared links and SEO
# signals point at the real domain instead of the *.vercel.app fallback.
NEXT_PUBLIC_SITE_URL=https://www.uvicmsa.example
